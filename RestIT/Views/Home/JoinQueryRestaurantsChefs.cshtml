@model IEnumerable<RestIT.Controllers.HomeController.RestaurantChefs>
@{
    ViewData["Title"] = "Join Query";
}

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RestName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RestCity)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ChefName)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RestName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RestCity)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ChefName)
        </td>
    </tr>
    }
</table>
