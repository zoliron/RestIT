@model IEnumerable<RestIT.Controllers.HomeController.GroupByRestaurant>
@{
    ViewData["Title"] = "GroupBy RestaurantCities";
}

<script src="https://d3js.org/d3.v5.min.js"></script>

<style>
    .chart {
        fill: lightpink;
        stroke: lightsalmon;
        stroke-width: 0.5px;
    }
</style>

<h2> GroupBy Restaurant Cities</h2>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RestCity)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RestCount)
        </th>
        <th></th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RestCity)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RestCount)
            </td>
        </tr>
    }
</table>

@{
    var restaurantCities = ViewBag.data;
}

<div id="restaurantCities" hidden>@restaurantCities</div>

<div>
    <svg class="chart" name="countRestaurants" width="1000" height="400" onload="restaurantCitiesChart();"></svg>
</div>
